import 'package:flutter/material.dart';

class DioListScreen extends StatelessWidget {
  const DioListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dio List with GetX'),
      ),
      body: const Center(),
    );
  }
}
